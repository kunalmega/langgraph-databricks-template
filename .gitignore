# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# Per-workspace env files (keep the example). Also keeps `databricks sync` from
# uploading them to the workspace, since sync respects .gitignore.
.env.*
!.env.example

# Node
node_modules/
npm-debug.log*

# Build outputs (keep frontend/dist for deployment!)
# frontend/dist/  # DO NOT exclude this!

# IDE
.idea/
.vscode/
*.swp

# Databricks
.databricks/

# Isaac tooling
.isaac/

# OS
.DS_Store

# MLflow
mlruns/
uv.lock

# Customer bundle build artifact
langgraph-databricks-template.zip
